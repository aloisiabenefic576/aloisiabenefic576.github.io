const progress=document.getElementById('progress'),sticky=document.querySelector('.sticky');const setProgress=()=>{const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h>0?scrollY/h*100:0)+'%';if(sticky)sticky.classList.toggle('show',scrollY>420)};addEventListener('scroll',setProgress,{passive:true});setProgress();
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const crisisSteps=[...document.querySelectorAll('.crisis-step')];const stepIo=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){crisisSteps.forEach(s=>s.classList.remove('active'));e.target.classList.add('active')}}),{rootMargin:'-35% 0px -45% 0px',threshold:0});crisisSteps.forEach(el=>stepIo.observe(el));if(crisisSteps[0])crisisSteps[0].classList.add('active');
const form=document.getElementById('calcForm'),range=document.getElementById('range'),saving=document.getElementById('saving'),time=document.getElementById('time');const money=n=>new Intl.NumberFormat('ru-RU').format(Math.round(n/1000)*1000)+' ₽';function calculate(){const d=new FormData(form);const area=Math.max(.2,Number(d.get('area'))||.2),pieces=Math.max(1,Number(d.get('pieces'))||1);let base=Math.max(4000,(area*6200+pieces*1800)*Number(d.get('object'))*Number(d.get('damage'))*Number(d.get('depth'))*Number(d.get('access'))*Number(d.get('urgency')));const low=base*.88,high=base*1.17,replacement=Number(d.get('replacement'))||0;range.textContent=money(low)+' — '+money(high);saving.textContent=replacement>0?money(Math.max(0,replacement-high)):'уточним после фото';time.textContent=Number(d.get('urgency'))>=1.4?'день в день':area<=6?'от нескольких часов':area<=20?'1–3 дня':'от 3 дней';return{low,high,replacement}}form.addEventListener('input',calculate);calculate();document.getElementById('sendCalc').addEventListener('click',()=>{const r=calculate(),d=new FormData(form);const text=`Здравствуйте. Хочу получить оценку работ.\nТип объекта: ${form.object.options[form.object.selectedIndex].text}\nПлощадь: ${d.get('area')} м²\nЭлементов: ${d.get('pieces')}\nПовреждение: ${form.damage.options[form.damage.selectedIndex].text}\nПредварительный диапазон: ${range.textContent}\nЦена замены: ${r.replacement?money(r.replacement):'не указана'}`;open('https://t.me/zhukov_boss?text='+encodeURIComponent(text),'_blank')});